#содержит <stdio.h>
// коммент

целое4 точка_входа(целое4 количество_аргументов, символ* [] аргументы)
{
    printf(аргументы[1]);
    целое4 x = 1;
    x ++;
    если (x>1) {
        printf("x>1");
    }
    размер_тип y = 0;
    y = размер(x);
}
