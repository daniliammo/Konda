/* Сгенерировано транспилятором Konda */
#include <stdint.h>
#include <stdio.h>
int32_t main(int32_t argc, char **argv)
{
    if (argc > 1) {
        printf("%s\n", argv[1]);
    } else {
        printf("%s\n", "аргументов нет");
    }
    int32_t икс = 1;
    return 0;
}
