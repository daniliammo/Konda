# CLAUDE.md — рабочие правила и решения проекта Konda

Транспилятор `.конда` → C. Русскоязычный язык с целью: **программист не может
допустить ошибку управления памятью**, при этом в релизе — скорость уровня C.

## ГЛАВНОЕ ПРАВИЛО: фиксировать важное в документации

Всё, что должно пережить `/compact` и не «потеряться» между сессиями, **сразу
записывается в документацию или в этот файл** — не держим важное только в
контексте диалога:

- **Принятое пользователем решение** (в т.ч. ответы на AskUserQuestion), смена
  направления, инвариант дизайна → полным текстом в `docs/РЕШЕНИЯ.md` (новый
  раздел `## §N. Название`, увеличь номер) **И** строкой-сводкой в раздел
  «Принятые решения (сводка)» этого файла. Так always-loaded CLAUDE.md остаётся
  компактным, а обоснования читаются по требованию (дёшево, по `§N`).
- **Реализованная возможность / архитектурный факт** → в `README.md`
  (пользовательский взгляд) и/или `docs/АРХИТЕКТУРА_ТРАНСПИЛЯТОРА.md` (внутренний).
- Обновляя код, **сразу актуализируй доки** (в т.ч. §«Что уже/пока не
  поддерживается» и «Ближайший план» в README) — устаревшая дока хуже её
  отсутствия.
- Перед изменением синтаксиса — **сначала расширить тесты** (`tests/run.sh`).

## ОБЯЗАТЕЛЬНО: TODO-лист на каждое задание

На **каждое** задание пользователя (кроме тривиального вопроса в одно действие)
**сразу** составляй план через `TodoWrite` и веди его: один пункт `in_progress`,
отмечай `completed` по мере готовности. Это не опция — план обязателен даже если
задача кажется простой (раскрывается по ходу). Для фичи-«вертикального среза»
пункты обычно: лексер/парсер → семантика → кодоген → тесты `run.sh` → доки. и коммит если фича готова к пушу на гитхаб

## Экономия токенов (держать сессию дешёвой)

- **Не перечитывай файл после `Edit`/`Write`** — правка уже подтверждена, харнесс
  держит состояние. Перечитывай только если реально изменил не то место.
- **Читай узко:** `grep -n` → `Read` с `offset`/`limit` по нужному диапазону, а не
  файл целиком. `tests/тело.sh` (12k строк — ВСЕ тесты), `семантика.c` (7k),
  `кодоген.c` (3.5k) — никогда не читать целиком; секции тестов: `grep -nE '^# ─'
  tests/тело.sh` (фича → строка). Драйверы `run.sh`/`run_seq.sh` — маленькие.
- **Решения — по `§N`** из `docs/РЕШЕНИЯ.md` (см. выше), не весь файл.
- Не дублируй в ответе то, что и так во вьюхе diff/тест-логах; резюмируй кратко.

## Инварианты дизайна (не менять без явного согласия пользователя)

1. **Безопасность — статическая, нулевая рантайм-цена.** Любая новая проверка
   безопасности — на этапе компиляции (или тип), чтобы **релиз работал так же
   быстро, как C**. Единственная рантайм-проверка — границы срезов/массивов/argv
   (её сжимаем статическими доказательствами, а не убираем).
2. **Небезопасное — только в `небезопасно { }`** (счётчик `глубина_небезопасно`,
   в C — обычный блок, нулевая цена). За гейтом: сырой указатель
   (индексация/арифметика), реинтерпретация указателя, доступ к члену `союз`а,
   небезопасные функции libc.
3. **`printf` НЕ трогать** — пользователь позже сделает свою stdlib; формат-строка
   передаётся как есть по дизайну.
4. **Глобальных изменяемых переменных НЕТ — КРОМЕ атомиков `атом<T>`** — верхний
   уровень принимает директивы, функции, пространства, типы, `конст`-константы и
   `атом<T>`-атомики. Ключевая формулировка инварианта — не «нет разделяемого
   изменяемого состояния», а «нет **гонок ДАННЫХ**» (memory-safety). Обычные
   изменяемые глобалы дали бы гонки данных (torn reads/writes, UB) → запрещены.
   `атом<T>` (`_Atomic` в C, целочисленный примитив) делает разделяемое состояние
   называемым, но каждый доступ **атомарен** → гонок ДАННЫХ нет по построению даже
   с потоками (проверено TSan); возможны лишь ЛОГИЧЕСКИЕ гонки (потерянные
   обновления при `x = x + 1` = load+store), а они не нарушают memory-safety — как
   `AtomicI32` в Rust. Так несущее свойство («гонки данных невозможны») цело.
   Обычный `целое32 g = 0` на верхнем уровне — по-прежнему ошибка (и в парсере, и
   в семантике), с подсказкой про `конст`/`атом`. Решение пользователя (выбор из
   гейт-по-однопоточности / `атом` / оставить-как-есть) — `атом` (всегда
   безопасен, работает и с потоками).
5. **Максимизировать статические проверки.**

## Основные принципы (выведены из истории коммитов)

- **Фича = полный вертикальный срез в одном коммите.** Каждая возможность идёт
  сразу через `лексер → парсер → семантика → кодоген → IDE-hover` **плюс** тесты в
  `tests/run.sh` **плюс** обновление `README.md` и `docs/АРХИТЕКТУРА_ТРАНСПИЛЯТОРА.md`.
  В истории почти каждый коммит трогает все четыре — не оставляй фичу без тестов и
  доков. Ловушка парсера: токен, которым может ЗАКАНЧИВАТЬСЯ оператор, обязан быть
  в `токен_завершает_оператор`, иначе перевод строки после него съедается.
- **Предпочитать проверки без рантайма** («Дополнительные проверки без рантайма»,
  «больше статических проверок») — статика вместо рантайм-стоимости везде, где
  можно; рантайм-guard'ы (деление/сдвиг/переполнение) — только в отладке.
- **Безопасность памяти — непрерывная работа.** Регулярные фиксы double-free,
  деструкторов, use-after-move, transitive-destruction; каждый — с тестом-регрессией.
- **Урок (17.07): любой анализ, снимающий проверку, обязан видеть ВСЕ способы
  мутации.** `собрать_изменяемые` собирал только присваивания и не видел
  `изменяемый`/`вывод` — переменная считалась «известной константой», проверка
  границ снималась по ложному основанию, ASan ловил heap-buffer-overflow.
  Мутация без присваивания в коде — это `изменяемый`/`вывод` (и только они:
  глобалов и сырого `&` нет). Тесты — `мутация_вывод`/`мутация_изм`.
- **Безопасен и сам транспилятор.** `cc` запускается через `execvp` с готовым argv
  (не `system()` со строкой — закрыт command injection через имя файла); парсер
  защищён от переполнения стека на патологических выражениях.
- **Русские ключевые слова, контекстные где нужно.** `и`/`или` вместо `&&`/`||`;
  `и` — контекстное (остаётся валидным именем переменной-счётчика). C-запись тоже
  принимается. Новое ключевое слово не должно без нужды отнимать имя у программиста.
- **Диагностики — на русском, с позицией** (`строка`/`колонка` из `Токен`), и в
  **батч-режиме**: семантика собирает все ошибки за прогон (парсер пока стоп-на-первой).
  **Цветной вывод (CLI, `основа.c`):** ошибки — красным, предупреждения — жёлтым,
  плюс строка-источник с кареткой `^` под колонкой. Цвет — только когда `stderr`
  терминал и не задан `NO_COLOR` (при перенаправлении/в тестах — чистый текст,
  прежний формат заголовка «Ошибка транспиляции»/«Предупреждение» сохранён, тесты
  грепают как раньше). Каретка выравнивается по СИМВОЛАМ (UTF-8 продолжающие
  байты столбец не двигают — как в лексере), иначе кириллица сдвигала бы её.
  Сниппет-источник показывается для одиночного файла (при multi-file
  пост-слияние теряет файл-локальность строки — только заголовок). `libkonda_ide`
  НЕ трогаем: он отдаёт структурированные диагностики, терминальную печать не
  делает.
- **Нет сырого `&`.** Мутация переменной вызывающего — через `изменяемый`/`вывод`
  (ref/out, кодоген сам ставит `&`/`*`); возврат указателя — только borrow
  (память переживёт возврат). Так исключены висячие указатели без borrow-checker'а.
- **IDE-first.** Есть стабильный API `libkonda_ide.c` (анализ в памяти без файлов и
  без `cc`) для language server — не ломать его сигнатуры при правках семантики.

## Принятые решения (сводка — детали в `docs/РЕШЕНИЯ.md`, ищи `§N`)

Полный журнал (обоснования, инварианты реализаций, ответы на AskUserQuestion) —
`docs/РЕШЕНИЯ.md`. **Дёшево прочитать одно решение:** `grep -n "§7\." docs/РЕШЕНИЯ.md`
→ строка, затем Read узким диапазоном (весь файл читать не нужно). Ниже — «что
решено» одной строкой (этого хватает, чтобы не заходить повторно на отклонённое):

- **§1 Null-безопасность** — `T*` ненулевой по умолчанию; nullable = `возможно<T*>`, разворот под охранником; добавлен `!=`.
- **§2 Охват `возможно`** — только указатели; опция-над-значениями отложена.
- **§3 `возможно<T*>` над именованными типами** — да (структура/перечисление/внешний тип), для opaque-хендлов C.
- **§4 Конвертер C→Konda** — отдельный проект `konda-from-clang`; в этом репо конвертера НЕТ.
- **§5 Интерфейс библиотек (путь 2)** — типизированный `.и`; транспилятор сам линкует `.so` (rpath).
- **§6 Макросы** — только безопасная условная компиляция; функциональных макросов НЕ делать.
- **§7 Имена в C** — как есть, без манглинга/транслитерации (C23 UTF-8-идентификаторы).
- **§8 Побитовые `& | ^ << >> ~`** — реализованы (целые, нулевая цена).
- **§9 Перегрузка операторов** — реализована (≥1 операнд-структура, по значению; `&&`/`||` нельзя).
- **§10 Потоки** — модель «передача владения»; гонки данных невозможны по построению; shared-memory отвергнута.
- **§11 Каналы `канал<T>`** — ОТЛОЖЕНЫ осознанно (рантайм-либа + дедлоки).
- **§12 Async/await** — ОТЛОЖЕНА (рантайм несопоставимо больше каналов); падает вместе с каналами.
- **§13 Типизированные слушатели `слушатель<T>`** — ✅ (доверенный трампулин, у пользователя нет `void*`/каста); фаза 2 (время жизни) отложена.
- **§14 Интероп с C** — 4 конструкции: `внешняя`, `внешний тип`, `чтение`, глобальные `конст` (+ конст-массивы); все нулевой цены.
- **§15 Батч-ошибки парсера** — реализованы (восстановление на границе объявления).
- **§16 Обобщённые структуры** — реализованы (мономорфизация); поля v1 — НЕ `срез<T>`.
- **§17 Вывод тип-аргументов** — реализован (гибрид, отдельный проход `режим_вывода`); только из конкретных аргументов.
- **§18 Cross-file generics** — реализованы (сбор имён обобщённых по всем файлам до разбора).
- **§19 `goto`** — НЕ реализовывать (конфликт с definite-assignment/RAII/move); альтернатива — помеченные `прервать`/`продолжить`.
- **§20 Оптимизации `Срез`/`выделить` (A/B/C)** — реализованы; D (элизия зануления) сознательно НЕТ.
- **§21 Ссылки на проекции** — реализованы (second-class ref, без borrow-checker); дальше НЕ идти.
- **§22 `срез<структура>`/`срез<T*>`** — реализованы; срез структур с владеющими полями — ошибка.
- **§23 `Ящик<T>`** — одиночный владеющий heap-объект; T без владеющих полей (v1).
- **§24 Аллокатор `--аллокатор=jemalloc|libc`** — дефолт jemalloc (интерпозиция линковкой); ASan → libc.
- **§25 Кросс `--целевая-архитектура=<триплет>`** — реализована (пока gcc); на кроссе авто-libc.
- **§26 `--только-си` (`--only-c`)** — реализован.
- **Статическая линковка** — `--статический` (`--static`): `cc -static`, линкует jemalloc как `.a` (нужен `libjemalloc-dev`); несовместим с `--библиотека`.
- **`размер_обьекта(тип)`** — sizeof(type) в C: примитивы, указатели, именованные типы.
- **§27 Бэктрейс при аварии** — реализован; всегда вшит; `-rdynamic/-g/unwind` вкл (снимает `--без-символов`).
- **§28 Hardening-флаги (Tier 2)** — 5 флагов нулевой цены; `-fno-strict-aliasing`/`_FORTIFY` отложены.
- **§29 Безопасность рекурсии** — защита стека (SIGSEGV на alt-стеке) + гарант. TCO (`КОНДА_ХВОСТ`) + предупреждение по флагу.
- **§30 Кастомный аллокатор** — пер-тип (`Ящик<T> в Имя`, `срез<T> в Имя`), пул во внешней памяти (инвариант №4 цел), autofree через хук; границы среза целы; cross-boundary передача владения запрещена (v1).
- **§31 Символьные литералы `'x'`** — тип `символ` (не `int`); лексер уже собирал `ТОКЕН_СИМВОЛ`, добавлены парсер/семантика/кодоген (эмит дословно); escape ок; пустой `''` — ошибка; v1 — ASCII.
- **§32 Мини-stdlib `stdlib/файл`** — 3-слойная модель (POSIX ← C-шим ← Konda-обёртки): `Срез` по значению в шим → границы гарантированы, `[удалить]` даёт RAII, sum-типы заменяют errno. Пользовательский код БЕЗ `небезопасно`. Тест — под ASan чисто.
- **§33 Обобщённые sum-типы** — `перечисление Результат<T, E> { Успех(T) Провал(E) }`. Мономорфизация параллельна структурам (обобщения.c: третья таблица `enum_шаблоны` + фаза 3в). Cross-file уже подхватился. **v1-ограничение**: одна специализация на шаблон (иначе конструкторы вариантов неоднозначны без вывода типа-контекста) — понятная ошибка. stdlib унифицирован на `Результат<целое64, целое32>`.
- **§34 Именованные типы через «.и» (v2 пути 2)** — структуры (поля+`[удалить]`) и sum-перечисления экспортируются в `.и` (`С`/`Поле`/`Е`/`Вар`/`ВП`); потребитель реконструирует их как штатные узлы (`из_библиотеки`): литералы, `выбор`-распаковка, `изменяемый`-сверка типа по имени, RAII-деструктор у потребителя; typedef не эмитится (из `.h`). C-определения — из `.h`; `типфункции`/пространства/обобщения — пока непрозрачны.
- **§35 Автоподключение stdlib** — для программы stdlib находится сама (рядом с бинарником или `KONDA_STDLIB`), подключает функции/типы из `.и` + синтетический `#содержит "файл.h"` + автолинк `.so`. Отключение: `--без-стандартной-библиотеки`, `--библиотека`, `--статический` (v1-ограничение: stdlib недоступна), локальные `#содержит "…"`, коллизия имён («Ф имя»/«Вар имя» из `.и` vs корень; варианты сум-шаблонов = функции-конструкторы). Предупреждение при коллизии. Урок: UTF-8-литералы мерить в БАЙТАХ («Вар »=7, «Ф »=3) — и не гонять на несвежих бинарниках.
- **§36 Аргументы точки входа = `срез<символ*>`** — сигнатура теперь одна: `целое32 точка_входа(срез<символ*> аргументы)` (argv включает имя программы, `длина(аргументы)`==argc, заимствованный срез, borrow-возврат). Старая форма `(целое32 количество_аргументов, символ** аргументы)` и пустая `()` — снята (ошибка семантики). Кодоген синтезирует `Срез аргументы = {argv,argc}` в прологе main; удалены ремап имён, `аргументы_индекс`, argv-«связи». Заодно `срез<T*>`/`срез<Структура>` заработали как ТИП ПАРАМЕТРА. Компромисс: литеральный OOB argv под охранником больше не статический — рантайм-проверка среза (memory-safety цела). Любой main теперь тянет typedef `Срез`+`срез_доступ`.
- **§38 Условия/циклы без `(…)`** (Rust-стиль) — `если`/`иначе если`/`пока`/`для`/`для_каждого` пишутся без обрамляющих скобок вокруг условия/заголовка; тело начинается с `{` (неоднозначности нет: `{`-литералы только в позиции инициализатора). Парсер стал проще (убраны обязательные `съесть(круглая)`). `если (x)`/`пока (x)` ещё валидны (скобочное выражение, `== если x`), а **старая форма `для (…)`/`для_каждого (…)` снята** (ошибка разбора). Тесты/фикстуры/доки мигрированы; тесты `без_скобок`/`скобки_условия_ок`/`для_скобки_снято`.
- **§41 C23 `выровнять(N)`/`встроить("файл")`** — `выровнять(N)` = alignas(N) (ключевое слово, ведущий спецификатор объявления/поля структуры; N — степень двойки, проверка в семантике; кодоген `alignas(N)`; 0 рантайма). `встроить("файл")` = #embed: `конст неподписанный целое8 имя[] = встроить("путь")` → `static const uint8_t имя[] = {#embed "путь"}` (контекстное слово; пустые `[]`=`выводимый_размер` только с встроить; элемент строго `неподписанный целое8`). Длину знает C (sizeof) → индексация ВСЕГДА рантайм-`массив_доступ` (обобщён индексный путь: `база->массив` без `длина_известна`; детектор пролога не исключает числовой индекс). Путь #embed резолвится через `--embed-dir=<катал.источника>` (ставится лишь когда есть встроить — `программа_использует_встроить`; #embed ищет рядом с .c и по --embed-dir, но НЕ по -I). Требует cc с C23. Тесты `выровнять_*`/`встроить_*`.
- **§40 Предопределённые макросы для `#если`** — режим сборки `дебаг`/`релиз` (по `--релиз`), ОС `линукс`/`макось`/`бсд`(+`фрибсд`/`опенбсд`/`нетбсд`/`драгонфлай`), архитектура `x86_64`/`арм64`/`рискви64`/`x86`/`арм`/`рискви`, разрядность `бит64`/`бит32` (определён только активный; неактивное имя = 0). Русские имена (выбор пользователя), полный набор (режим+ОС+арх+разрядность). Платформа из ТРИПЛЕТА цели при кросс-сборке, иначе из ХОСТА (compile-time макросы + `sizeof(void*)`). `макросы_предопределить()` в [макросы.c], вызов в [основа.c] после разбора аргументов, до `условная_компиляция`. `-D` побеждает предопределённый (`задать_если_нет`). Заодно в вычислителе `#если` добавлены слова-операторы `не`/`и`/`или` (= `!`/`&&`/`||`). Имена-макросы не отнимают имён у кода (таблица макросов отдельна). Ноль рантайма (ветки вырезаются до разбора).
- **§39 Портируемость сборки (Linux/BSD/macOS)** — транспилятор и вызываемые им `cc` (сборка `.конда`-программ/библиотек) переносимы по POSIX. Единая константа `КОНДА_СУФФИКС_БИБЛ` (`"so"`/`"dylib"`) в [транспилятор.h] — точка правды для эмита и авто-подключения stdlib. В `скомпилировать_си` ([ввод_вывод.c]) OS-гейтинг: `-Wl,-z,relro/-z,now/-z,noexecstack`, `-fstack-clash-protection`, `-rdynamic` — только на не-Darwin (ld64 их не знает); `-shared` vs `-dynamiclib`+`-install_name,@rpath/…`; jemalloc — `-l:libjemalloc.so.2` только на Linux (GNU-ld/LLD-расширение), иначе `-ljemalloc`; `--статический` на macOS отвергается понятно (нет статической libSystem). Makefile принимает `Linux/FreeBSD/OpenBSD/NetBSD/DragonFly` в ELF-ветке; Darwin — своя. Тесты `run.sh` пока Linux-only (используют `ldd`/`-l:name`); добавлен guard: Darwin-флаги не протекают на Linux.
- **§42 Срезы строк как в Python (`s[a:b]`)** — по умолчанию *заимствованный ВИД* в тот же буфер (`{данные+начало, конец−начало}`, 0 alloc/0 рантайма кроме границ; borrow как у §37/§21 — возврат вида за пределы жизни родителя ловится); владеющая копия — явно `копия(s[a:b])` (autofree). Индексы БАЙТОВЫЕ с проверкой UTF-8-границы (Rust-семантика, не кодоточки: O(1), консистентно с байтовой `длина()`; рассечение символа → `abort`). v1 = `s[a:b] s[a:] s[:b] s[:]` + отрицательные `s[-1:] s[:-2]` (`i<0→длина+i`); ШАГА нет (сломал бы zero-cost вид). Лексер: одиночный `:` → `ТОКЕН_ДВОЕТОЧИЕ` (только внутри `[…]`; `::`/`?:` не тронуты; имя не отнимает). AST `УЗЕЛ_СРЕЗ_ДИАПАЗОН` (дети `[база, начало?, конец?]`, флаги `есть_начало`/`есть_конец`). База обязана быть `срез<T>`; тип результата = тот же срез. Кодоген-хелпер `срез_подсрез(...)`. UTF-8-проверка только для `срез<символ>`. Не покрыто: шаг, `+`, `==`, юникод-итерация.
- **§43 Аудит-фиксы (безопасность+корректность+парсер)** — Ф1 (критично): элизия проверки границ (BCE) в каноническом цикле/guard-условии не видела мутацию индекса через `изменяемый`/`вывод` (тот же класс, что 17.07, но в `имя_изменяется_в`, не `собрать_изменяемые`) → heap-overflow (ASan). Фикс (реш. польз.: *точно по режиму параметра*): `имя_изменяется_в` получила `Анализатор*` и резолвит сигнатуру (`найти_сигнатуру_по_имени`), считая мутацией только передачу в `по_ссылке>0`; быстрый путь при вызове ПО ЗНАЧЕНИЮ цел. Ф2/Ф3/Ф-float: guard'ы деления/сдвига считали в знаковом `long` → беззнаковое 64-бит деление/сдвиг неверны в ОТЛАДКЕ (release был прав), вещественное деление гейтилось и усекалось. Фикс (реш.: *проверка+нативное выражение*): хелперы `конда_чек_делителя`/`конда_чек_сдвига` только ПРОВЕРЯЮТ, операция — в родном типе через stmt-expression + `typeof` (single-eval); float исключён из guard (`тип_целочисленный(рез)`; ÷0 float определён). Ф4: `срез_клон` при OOM → `abort` (как `срез_выделить`). Парсер: батч теперь и на границе ОПЕРАТОРА внутри тела (реш.: *на уровне операторов*; `восстановиться_на_операторе` + ветка в `разобрать_блок`, предел 32) — все синтакс-ошибки тела за прогон. Отложено польз.: сканер заголовков и stdlib. Снято из планов: транслитерация в ASCII.
- **§44 Null-safety: возврат `возможно<T*>` и TOCTOU сужения** — nullability ВОЗВРАТА функции не отслеживалась → результат nullable-вызова считался ненулевым: `возможно<Узел*> п = найти(5); п.знач` (без охранника), `найти(5).знач`, `Узел* п = найти(5)` компилировались; **TOCTOU**: `если (п != нуль){ п = найти(9); п.знач }` дереферил возможный нуль. Фикс: `СигнатураФункции.возврат_возможно` (= `функция->возможно`) + общий предикат `выражение_может_быть_нуль` (нуль-литерал | несужённая `возможно`-переменная | вызов с `возврат_возможно`), применён на реассайне/объявлении/прямом дерефе. Non-null-цель отвергает любое nullable-значение; реассайн в nullable под охранником сбрасывает сужение. 0 рантайма (инв. №1). v1: библиотечные (`.и`) и функц-указатели бит не несут; nullability поля-структуры не отслеживается. Тесты `нуллабл_вызов_*`.
- **§46 `цикл { }` (Rust `loop`) + CLI `--код=<исходник>`** — (1) `цикл { тело }` — бесконечный цикл, **контекстное** слово (распознаётся лишь как `цикл`+`{`; иначе имя переменной цело). Чистый десугар в парсере → `пока истина { тело }` (0 правок семантики/кодогена): наследует `прервать`/`продолжить`, `while(true)` и **анализ дивергенции** (как Rust: `цикл` без `прервать` с `вернуть` внутри — расходится, возврат после не нужен; с `прервать` — нужен). Тесты `цикл_*`. (2) `--код=<исходник>`/`-e`/`--код <исх>` — исходник строкой-аргументом (быстрые тесты, меньше I/O: нет записи `.конда`). Имя вывода — `--имя=` (дефолт `код`). v1: один синтетический файл; `--код`+файл — ошибка. `.c`/`.elf` пишутся (нужны `cc`). Тесты `--код`/`-e`/`--имя`. [основа.c].
- **§45 Составное присваивание `+= -= *= /= %= &= |= ^= <<= >>=`** — чистый десугар в парсере `цель ОП= знач` → `цель = цель ОП знач` (реюз всей машинерии биноп: guard'ы §43, нативный тип, перегрузка операторов, проверки типов/`конст`/гейт указателей — 0 правок семантики/кодогена). Лексер: `ТОКЕН_СОСТАВНОЕ_ПРИСВАИВАНИЕ` + жадная 3-символьная ветка для `<<=`/`>>=`. **Double-fetch/TOCTOU закрыт**: цель эмитится дважды → парсер ОТВЕРГАЕТ цель с вызовом (`с[f()] += 1` — ошибка). Семантика как `цель = цель ОП знач` (сужение `целое8 += 20` требует `как<>()`, единообразно). `++`/`--` — отдельно (§47). Тесты `составное_*`.
- **§51 F-строки: быстрый срез-путь (borrow) + ошибка пустой интерполяции** — (1) `срез<символ> в = "\(имя)"` (единственная вставка `символ*`) больше НЕ копирует в `snprintf`-буфер, а отдаёт заимствованный ВИД `({ const char *_s = (имя); (Срез){(void*)_s, strlen(_s)}; })` — без буфера/копии, single-eval, borrow (не владеет, autofree не ставится; консистентно с §42; возврат по-прежнему запрещён; проверено ASan). Единый предикат `fстрока_нужен_буфер = !fстрока_только_строка` — одна точка правды для пролога и кодогена (иначе рассинхрон `_kfN`). (2) Пустая/пробельная интерполяция `"\()"` / `"\(  )"` — теперь ошибка транспиляции с ПОЗИЦИЕЙ строки («пустая интерполяция… встройте значение или уберите `\(…)`»), а не прежнее «строка 0, колонка 0: неожиданное выражение» из под-парсера. Тесты `фстрока_срез_быстро`/`фстрока_пусто`/`фстрока_пробел`.
- **§50 Право-размерные буферы f-строк** — вместо жёстких `char _kfN[1024]` на каждую f-строку кодоген СТАТИЧЕСКИ считает размер по типам вставок (`размер_fбуфера`/`граница_вставки`): сумма верхних оценок вывода (`int`→11, `int64`→21, `символ`→1, `%p`→18, `double`→330) + сырые длины чанков + NUL. Строковая вставка `%s` безразмерна → прежние 1024 (усечение, memory-safe, поведение как было). `посчитать_fбуферы`→`эмит_декл_fбуферов` (предпорядок совпадает с раздачей имён `_kfN`). 0 рантайма (инв. №1). ASan-прогон зелёный (ни одна оценка не мала). Экономит стек в горячих циклах. Отложено: быстрый путь `срез<символ>` для одиночной строковой вставки; фолдинг литеральных f-строк. Тест `фстрока_размер`.
- **§49 memory_order для атомиков** — `атом<T>` голый доступ = seq_cst (дорого на ARM64). Добавлены встроенные ФУНКЦИИ (не методы — их в языке нет): `загрузить(а, порядок)`/`сохранить(а, знач, порядок)`/`обменять(а, знач, порядок)` → `atomic_load/store/exchange_explicit(&а, …, memory_order_X)`. Порядок — 5 КОНТЕКСТНЫХ имён (`расслабленный`/`получить`/`освободить`/`получить_освободить`/`последовательный`, имён у кода не отнимают). Диспатч ТОЛЬКО когда 1-й аргумент реально атомик (иначе `загрузить(x)` — обычная польз. функция); семантика метит `узел->атом=1`, кодоген эмитит + тянет `<stdatomic.h>`. Проверки: порядок допустим для операции, значение без сужения/влезает в T. Голый доступ = seq_cst (совместимость, TSan-зелёно). v1: только именованный атомик, нет compare_exchange, атом-операция нечистая (не в `параллельно для`). Тесты `атом_порядок*`.
- **§48 Релиз-флаги `-fno-math-errno` + `-fno-plt`** — только в релизе (`if (релиз)` в `скомпилировать_си`, [ввод_вывод.c]). Оба снимают ИСКУССТВЕННЫЕ барьеры оптимизатора (не проверки → инвариант №1 цел). `-fno-math-errno` разрешает gcc векторизовать `sqrt/exp/log/pow` (иначе errno-побочка запрещает); `-fno-plt` заменяет PLT-thunk на `call *foo@GOTPCREL(%rip)` — минус инструкция на каждый внешний вызов, совместимо с §28 FULL RELRO (`-Wl,-z,now` уже задан). Отладка не тронута. Тест `флаги_сборки` расширен четырьмя ассертами.
- **§47 Постфиксный `x++`/`x--` (Go-стиль оператор)** — ТОЛЬКО оператор (не выражение; `y = x++` — ошибка), только postfix. Отдельный `УЗЕЛ_ИНКРЕМЕНТ`, НЕ десугар в `x = x + 1` (иначе `целое8 x; x++` → `целое8 + 1` → `целое32` → сужение). Кодоген — нативный `(цель)++`: C гарантирует single-eval, поэтому `с[f()]++` зовёт f() ОДИН раз (хойст даёт сам C). Отладка + знаковое целое → хойст-форма с `арифметика_безопасно` (в отладке нет `-fwrapv` → `INT_MAX++` без guard = UB); беззнак/вещ/релиз/указатель-в-`небезопасно` — нативно. Указатель++ вне `небезопасно` / логический / структура — ошибка. **Безопасность (класс §43 Ф1):** `имя_изменяется_в`+`собрать_изменяемые`+`искать_прямой_эффект`+`проверить_параллельное_тело` учитывают `++` (лишний `и++` в теле канонического цикла отменяет элизию границ; `счёт++` в `параллельно для` — гонка); шаг `и++` распознан как канонический (+1). Диспетчеры `анализ_оператора`/`кодоген_оператора` получили ветку. Тесты `инкремент_*`.
- **§37 Строки = `срез<символ>`** (тип/литералы в транспиляторе, операции позже в stdlib; минимум проверок = переиспользовать машинерию срезов, а не новый checked-тип). **Slices 1—5 реализованы:** (1) объявление литералом → заимствованный срез над `.rodata` (длина в БАЙТАХ UTF-8, NUL сохранён — совместимость с `printf`); (2) коэрция литерала/f-строки на call-site и в присваивании — с guards на владеющую цель, `владеет`-параметр и возврат (иначе `free(.rodata)`/dangling); (3) владеющие строки через `выделить(N)` (стандартный autofree срезов) и f-строки в срез-контексте — snprintf + Срез{стек-буфер, длина}, возврат такого — ошибка; (4) контекстный алиас `строка` = `срез<символ>` (позиция типа; имя-переменную не отнимает); (5) `вкси(с)` → `символ*` — zero-cost для литералов/f-строк (флаг `с_нулем` на Символе/узле, эмит `.данные` напрямую), копия+NUL в стек-буфер для прочих. Обратная совместимость полная — `символ*`-литералы не тронуты. **Slice 6 (§37 п.6): возврат заимствованных строк через ВЫВОД владения возврата** — без keyword, без рантайма. Пред-проход `вывести_владение_возврата` (как `вывести_чистоту`, с fixpoint) классифицирует функцию: все возвраты — литералы/`вернуть G()`-borrow → *заимствует-возврат* (бит в сигнатуре); есть возврат из `выделить`/владеющей локали → *владеющая*; смешанная → ошибка (единообразие, копию не навязываем). Семантика переносит бит на call-узел; кодоген на `срез с = ф()` регистрирует autofree ТОЛЬКО если бит=0 (компайл-тайм, 0 рантайма). Фабрика литералов (`строка код(){ вернуть "OK" }`) — **0 alloc, 0 free**. Cross-library: бит в `.и` (`Ф имя <бит>`; старые `.и`→0=владеющий). Возврат borrow-ПАРАМЕТРА пока ошибка (отдельный слайс). Не покрыто: конкатенация, подстроки, `==`, юникод-итерация, операции в stdlib.

## Приоритеты дальше (безопасность уже в хорошем состоянии — дальше полнота)

1. ✅ **Sum-типы с данными** — реализованы: `перечисление Имя { Вариант(тип имя…) … }`
   (поле варианта обязано быть ИМЕНОВАННЫМ — оно становится полем union в C;
   безымянное раньше молча давало битый C, теперь ошибка Konda с позицией)
   + привязка payload в ветках `выбор`; кодоген — размеченный союз (tag+union),
   нулевая рантайм-цена; поля варианта — примитивы/указатели (без `срез`).
   Дальше на их основе — `Результат<T,E>` и опция-над-значениями (`возможно<значение>`).
2. ✅ **Индексация произвольного lvalue** — реализована для срезов
   (`структура.поле[i]`, цепочки `а.б[i].в[j]`); проверка границ через `срез_доступ`.
   `функция()[i]` запрещён (утечка владеющего временного — присвоить в переменную).
3. ~~Транслитерация имён в ASCII~~ — **снято** (пользователь против манглинга/
   неявных имён; держим кириллицу как есть, C23 UTF-8-идентификаторы).
4. Полнота: ✅ **обобщённые структуры**, ✅ **вывод тип-аргументов**,
   ✅ **cross-file generics** и ✅ **батч-ошибки парсера** (восстановление на
   границе верхнего уровня И оператора внутри тела) — реализованы (см. «Принятые
   решения»); осталось — массивы (вывод размера, локальный `срез<T> с = массив`).

**Параллелизм — статус и роадмап: `ПЛАН_ПАРАЛЛЕЛИЗМА.md`** (там же — чего стоит
каждый следующий шаг и какой инвариант затрагивает). Кратко:
✅ **Потоки** (модель передачи владения, см. «Принятые решения»): `поток<T>`,
`запустить(ф, арг)`, `присоединить(п)` + **авто-join** дескриптора при выходе из
области (RAII, как autofree срезов).
✅ **`параллельно для`** — параллелизм данных: диапазон дробится на
непересекающиеся куски, общения между потоками нет вовсе. Гонки невозможны по
построению; проверено TSan (0 гонок на 10⁶ элементов).
✅ **Редукция** (`редукция(итог)`) — приватный аккумулятор на кусок + свёртка
после join; только целые (float не ассоциативен → результат зависел бы от числа
ядер). Ею map-reduce стал выразим.
✅ **Вызовы чистых функций** в теле `параллельно для` — чистота **выводится**
(глобалов нет → эффект возможен только через `изменяемый`/`вывод`/срез-параметр/
указатель/`небезопасно`/вызов нечистой), новых ключевых слов нет.
Каналы и async — **отложены осознанно** (см. «Принятые решения»).
⚠️ Ограничения v1 (не дыры в безопасности — всё ловится компилятором): число
потоков — константа в исходнике; поток, объявленный в цикле, последователен
(есть предупреждение); ≤ 1 аргумент; `срез` как результат не поддержан.

## Сборка, тесты, санитайзеры

### Ловушки окружения (сверься ПЕРЕД правкой сборки/тестов — сэкономит цикл CI)

- **Набор — POSIX `sh` (dash), НЕ bash. Модульная структура (с этой сессии):**
  `common.sh` (хелперы `fail`/`assert_eq`, `SAN_MARKERS`, `check_compiler` +
  САНИТАЙЗЕР-функции `build_kasan`/`sanitize_corpus`), `тело.sh` (ВСЕ тест-секции —
  правь его, добавляя тест), `split_shards.py` (нарезка тела на шарды). Два
  драйвера подключают одно тело: **`run.sh` — параллельный (по умолчанию, ~36 с)** —
  строит ASan-бинарь KASAN В ФОНЕ, шардит `тело.sh` на безопасных границах секций
  (где ни одна фикстура не «переезжает» через разрез — `split_shards.py` считает
  интервалы жизни `[def..посл.исп]`), изолированный `mktemp` на шард, `xargs -P`,
  фикстуры шардов сводятся в общий корпус; **санитайзер — ЦЕНТРАЛИЗОВАННО** на
  сведённом корпусе (`sanitize_corpus`: `xargs -P` балансирует лучше пер-шардового
  дробления — пробовали пер-шард, вышло МЕДЛЕННЕЕ из-за неравных кусков).
  **`run_seq.sh` — последовательный эталон (~65 с, детерминированный порядок —
  для отладки)**, то же `sanitize_corpus`. Паритет проверен: OK, ловля падений
  (тела и санитайзера), идентичный корпус (514 фикстур). Имена переменных
  **И ФУНКЦИЙ — только ASCII**:
  кириллическое имя переменной (`КОМП=cc`) dash трактует как **команду**, а
  кириллическое имя функции (`проверить()`) → «Bad function name». Никаких
  bashизмов: `[[ ]]`, массивы, `${PIPESTATUS[@]}`, `local`, `function`. (Русские
  строки в кавычках — ок; не ИМЕНА.)
- **В игре ДВА компилятора — не путать.** (1) `$(CC)` из `Makefile` собирает
  сам транспилятор; (2) транспилятор в `скомпилировать_си` ([ввод_вывод.c])
  вызывает СВОЙ `cc` (или `<триплет>-gcc`), чтобы собрать **сгенерированный
  `.elf`**. Решения про аллокатор / hardening-флаги / jemalloc / версию gcc /
  символы касаются ИМЕННО (2) — правь argv в `скомпилировать_си`, `Makefile`
  тут ни при чём. Для CI следствие: `cc` в системе должен быть нужной версией
  (одного `CC=` для `make` НЕ хватает — `.elf` собирает внутренний `cc`).
  Санитайзер-sweep'ы `run.sh` — исключение: они зовут `cc -fsanitize` напрямую,
  минуя (2).
- **Сборка транспилятора — `-Werror`, компилятор gcc ≥ 15 или clang ≥ 19** (C23,
  musttail). 0
  предупреждений обязательно; временно снять `-Werror` — `make WERROR=`.
  `-Wformat-truncation` есть только при `-O0` (ASan-прогоны), не при `-O2` — их
  `-Werror` не касается. Проверить путь «старого gcc» локально: `gcc-13` стоит
  (но без `-std=gnu23` → `-std=gnu2x`).
- **Язык всех артефактов — русский:** диагностики, комментарии кода, коммиты,
  доки, ответы пользователю. Коммиты — подробные, фича = полный вертикальный
  срез (см. «Основные принципы»).

```sh
make                       # сборка libkonda.so + CLI (0 предупреждений — ЖЁСТКО:
                           # CFLAGS несёт -Werror, варнинг = ошибка сборки.
                           # Отключаемо «make WERROR=» под новый компилятор.
                           # Проверено чистым на gcc-13/14/15 при -O2.)
cd tests && ./run.sh       # весь набор ПАРАЛЛЕЛЬНО (~40 с) → «OK: все тесты прошли»
cd tests && ./run_seq.sh   # тот же набор последовательно (~62 с, эталон для отладки)
# ASan/UBSan-сборка CLI (все LIB_SRC + основа.c):
cc -std=gnu23 -g -O0 -fsanitize=address,undefined \
   дин_массив.c токенизатор_лексер.c конвейер.c ввод_вывод.c аст.c разбор.c \
   обобщения.c семантика.c владение.c кодоген.c заголовки.c интерфейс.c макросы.c \
   libkonda_ide.c основа.c -o /tmp/kasan
```

**Санитайзер-прогон транспилятора в `run.sh` (автоматический).** В конце `run.sh`
собирается тот же ASan+UBSan-CLI и через него **бесплатно переганяется весь
корпус `.конда`** (все накопленные фикстуры + песочница `тест.конда`, **плюс
мультифайловые наборы** — путь слияния `слить_программы` и cross-file обобщения,
которые одиночный прогон не трогает; при добавлении мультифайл-теста продублируй
его набор в список `COMBOS`) в режиме
`--только-си`; падение — только на отчёте санитайзера (маркеры `AddressSanitizer`/
`runtime error:`/`SUMMARY:`), не на коде выхода (негативные фикстуры отвергаются
штатно). **Одиночные фикстуры прогоняются ПАРАЛЛЕЛЬНО** (`xargs -0 -P` по числу
ядер, per-file лог, ошибки в общий файл — как в .elf-фазе; прежде был
последовательный `for`-цикл ~6 с → ~2 с). Мультифайловые `COMBOS` остаются
последовательными (их 8), копят ошибки в тот же файл. Так покрыт **сам компилятор** (арена, AST, строки, UTF-8) — инвариант
«безопасен и сам транспилятор»; прежде санитайзился лишь сгенерированный C.
**jemalloc НЕСОВМЕСТИМ с ASan** (перехват malloc/free мимо интерцепторов) →
санитайзер-прогон форсирует `--аллокатор=libc` (при `--только-си` компиляции
выхода нет, но флаг задан явно как инвариант/страховка). Утечки (LSan) намеренно
**не** утверждаются (`detect_leaks=0`): транспилятор короткоживущий (арена +
выход), цель — use-after-free/overflow/переполнение. *Этот прогон сразу поймал
латентный UB: `&а->типыфункций[индекс_структуры]` образовывался при
`индекс_структуры == -1` (сигнатура фп-псевдонима не разрешена в проходе вывода
тип-аргументов) — исправлено гардом `< 0` до индексации, как в присваивании фп.*

**Санитайзер сгенерированного C (.elf) в `run.sh` (автоматический).** Сразу за
прогоном транспилятора — вторая ось: ловим ошибки **кодогена**. Транспилятор-
санитайзер только что заново сгенерировал `вывод/<имя>.c` для каждой фикстуры
(`--только-си` → **отладочный** режим, с guard'ами границ/деления/переполнения,
и с libc); скрипт пересобирает **каждый** такой `.c` напрямую через
`cc -fsanitize=address,undefined` и **запускает** его. Так проверяется, что
СГЕНЕРИРОВАННАЯ программа безопасна по памяти (индексация, срезы, деструкция,
потоки) — прежде так тестировалась ровно одна фикстура (`стек_срез`), теперь весь
корпус. **Почему именно отладочный вывод, а не «как оставили»:** релиз
(`--релиз`) по дизайну снимает guardّы (инвариант №1: скорость C), поэтому релиз-
вывод фикстуры вроде `переполнение_рантайм` содержит сырое переполняющееся
сложение — UBSan бы его поймал, но это **не дефект кодогена**, а принятый
компромисс; прогон транспилятора нормализует всё обратно в отладку, где guard
аккуратно `abort`'ит ДО UB (без маркера санитайзера). **Самоотбор без списков:**
исполняемые `.c` отличимы по эмитированному `int32_t main(` (библиотеки `main` не
имеют — отсеиваются grep'ом ДО cc, без заведомо падающей компиляции);
фикстуры-гарды штатно `abort`'ят (не маркер санитайзера, код выхода игнорируем);
`stdin` из `/dev/null` + `timeout` против блокировок. Фаза **распараллелена**
(`xargs -P` по числу ядер, `getconf _NPROCESSORS_ONLN`; per-file лог — свой,
ошибки копятся в общий файл, итог после xargs): была ~40 с из ~62 с всего
прогона (224 × (cc ~60 мс + запуск под ASan ~107 мс) последовательно), на
12 ядрах — ~6 с (весь прогон: ~62 с → ~26 с). jemalloc обходится тем же способом —
компиляция `вывод/*.c` напрямую через `cc` (libc), минуя точку с дефолтным
jemalloc.

Конвейер: `токенизатор_лексер.c` → `разбор.c` → `обобщения.c` (мономорфизация) →
`семантика.c` → `кодоген.c`; собран в `Собранное/libkonda.so`, CLI `основа.c`.
Детали — в `docs/АРХИТЕКТУРА_ТРАНСПИЛЯТОРА.md`, пользовательский обзор — в `README.md`.

