#содержит <stdio.h>
целое32 точка_входа(срез<символ*> аргументы)
{
    целое32 N = 1048576
    срез<символ> a = выделить(N)
    неподписанный целое64 h = 0
    для повтор в 0..300 {
        h = h + фнв1а(a)
    }
    printf("%lu\n", h)
    вернуть 0
}
