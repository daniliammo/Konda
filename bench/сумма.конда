#содержит <stdio.h>
целое32 точка_входа(срез<символ*> аргументы)
{
    целое32 N = 1000000
    срез<целое32> a = выделить(N)
    для i в 0..N { a[i] = i }
    целое64 s = 0
    для повтор в 0..60 {
        для i в 0..N { s = s + как<целое64>(a[i]) }
    }
    printf("%ld\n", s)
    вернуть 0
}
